{
    "typescript": [
        "var queue: number[] = [];",
        "for (var item of input) {",
        "    if (item.operation == 'Push')",
        "        queue.push(item.data);",
        "    if (item.operation == 'Pop')",
        "        queue.splice(0, 1);",
        "}"
    ],
    "java": [
        "ArrayList queue = new ArrayList();",
        "for (QueueOperation item : input) {",
        "    if (item.operation == \"Push\")",
        "        queue.add(item.data);",
        "    if (item.operation == \"Pop\")",
        "        queue.remove(0);",
        "}"
    ]
}